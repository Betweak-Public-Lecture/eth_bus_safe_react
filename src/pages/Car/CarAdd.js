import React from 'react'
import {Container, Row, Col, Table, Form, Button} from 'react-bootstrap';

export default function CarAdd({history, match}) {
  return (
    <Container style={{paddingTop: 60}}>
      <Row>
        <Col>
          <h1>차량 생성</h1>
        </Col>
      </Row>

      <Row>
        <Col>

        </Col>
      </Row>

    </Container>
  )
}
